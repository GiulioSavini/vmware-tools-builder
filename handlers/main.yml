---
- name: Run ldconfig
  ansible.builtin.command:
    cmd: ldconfig
  changed_when: true

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart vmtoolsd
  ansible.builtin.systemd:
    name: "{{ vmtools_service }}"
    state: restarted
    daemon_reload: true
